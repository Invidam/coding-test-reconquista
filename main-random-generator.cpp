#include <iostream>
#include <vector>
#include <ctime>

using namespace std;


int main() {

    std::srand(static_cast<unsigned int>(std::time(nullptr)));

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int TC;
    cin >> TC;


    return 0;
}